# Userland

This directory is reserved for the future user-space model.

The early MVP will run applications in a kernel-managed runtime, but this
directory keeps the repository layout ready for later ELF loading, process
isolation, and shared user-space services.

