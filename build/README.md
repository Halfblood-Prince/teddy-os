# Build

This directory defines the build output and staging layout.

Planned contents:

- EFI staging tree conventions
- ISO staging directories
- debug and release artifact locations
- release packaging metadata

